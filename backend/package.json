{
  "name": "fiado-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Fiado backend — World ID verification, nullifier store, policy engine (AUTO/ESCALATE), openLine signing",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "test": "tsx --test src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@worldcoin/idkit-server": "^1.1.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
